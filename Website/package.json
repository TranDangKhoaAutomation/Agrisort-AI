{
  "name": "htdocs",
  "version": "1.0.0",
  "description": "1. Copy `.env.example` to `.env` and fill DB/SMTP values. 2. Create database with `database.sql`. 3. Install dependencies:    - `composer install` 4. Open: `http://localhost/`",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
